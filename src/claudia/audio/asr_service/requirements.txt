# =============================================================================
# Claudia ASR Service Dependencies
# Python 3.8 (system), Jetson Orin NX (aarch64), CTranslate2 backend
#
# Install: pip3 install --user -r requirements.txt
#
# NOTE: faster-whisper uses CTranslate2, NOT PyTorch for inference.
#       No CUDA PyTorch wheel needed. silero-vad still uses torch for CPU VAD.
# =============================================================================

# ASR inference (CTranslate2 backend, 4x faster than PyTorch Whisper)
faster-whisper>=1.0.0

# Voice Activity Detection (uses torch internally, CPU only)
# torch is pulled as silero-vad dependency
silero-vad

# Audio processing
numpy
soundfile

# Testing (dev only)
pytest
pytest-asyncio
